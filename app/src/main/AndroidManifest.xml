<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shoppinglist">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="?attr/actionModePasteDrawable"
        android:label="@string/app_name"
        android:roundIcon="?attr/actionModePasteDrawable"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ShopItemActivity"
            android:windowSoftInputMode='adjustResize' />  <!-- чтобы кнопка поднималась вместе с клавиатурой-->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>